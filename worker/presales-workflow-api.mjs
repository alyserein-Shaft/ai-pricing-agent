import { authenticateLibraryActor } from "./library-auth.mjs";
import { collectProjectFacts } from "./dashboard-api.mjs";
import { derivePresalesWorkflow, PRESALES_WORKFLOW_VERSION } from "../app/domain/presales-workflow-engine.mjs";
import { buildQuotationEvidenceManifest } from "./quotation-evidence.mjs";
import { buildQuotationTerms, exportEligibleForQuotationIssue } from "../app/domain/quotation-authority.mjs";
import { loadCanonicalPricingTotals } from "./pricing-authority.mjs";
const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"private, no-store"}}),uid=p=>`${p}_${crypto.randomUUID()}`,now=()=>new Date().toISOString(),bodyOf=async request=>{try{return await request.json();}catch{return {};}};
const parse=(v,f={})=>{try{return JSON.parse(v||"");}catch{return f;}};
const digest=async value=>[...new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(JSON.stringify(value))))].map(v=>v.toString(16).padStart(2,"0")).join("");
const access=async(db,projectId,userId)=>db.prepare("SELECT p.*,dp.client,dp.consultant,dp.contractor,dp.location,dp.tender_number,dp.package_name,dp.currency,dp.manual_status,COALESCE(pm.role,CASE WHEN p.owner_user_id=? THEN 'Project Manager' END) role FROM projects p LEFT JOIN project_dashboard_profiles dp ON dp.project_id=p.id LEFT JOIN project_members pm ON pm.project_id=p.id AND pm.user_id=? AND pm.status='Active' WHERE p.id=? AND p.organization_id IS NOT NULL AND (p.owner_user_id=? OR pm.id IS NOT NULL)").bind(userId,userId,projectId,userId).first();
const pricingTotals=async(db,projectId,currency="SAR")=>{
 const authority=await db.prepare("SELECT selected_pricing_scenario_id FROM project_dashboard_profiles WHERE project_id=? AND deleted_at IS NULL").bind(projectId).first();
 const scenarioId=authority?.selected_pricing_scenario_id||null;
 if(!scenarioId)return {currency,costMinor:0,subtotalMinor:0,lineCount:0,selectedScenarioId:null};
 return loadCanonicalPricingTotals(db,{projectId,scenarioId,currency});
};
const hydrateQuotation=row=>row?{...row,terms:parse(row.terms_json,{}),termsProvenance:parse(row.terms_provenance_json,{}),sourceSummary:parse(row.source_summary_json,{}),evidenceManifest:parse(row.evidence_manifest_json,null)}:null;
const quotationState=async(db,projectId,fingerprint)=>hydrateQuotation(await db.prepare("SELECT * FROM project_quotation_revisions WHERE project_id=? AND quotation_fingerprint=? AND superseded_at IS NULL ORDER BY revision_number DESC LIMIT 1").bind(projectId,fingerprint).first());
const latestQuotation=async(db,projectId)=>hydrateQuotation(await db.prepare("SELECT * FROM project_quotation_revisions WHERE project_id=? AND superseded_at IS NULL ORDER BY revision_number DESC LIMIT 1").bind(projectId).first());
export const loadPresalesWorkflowContext=async(db,project)=>{const baseFacts=await collectProjectFacts(db,project.id),totals=await pricingTotals(db,project.id,project.currency||"SAR"),evidence=await buildQuotationEvidenceManifest(db,project.id),projectView={id:project.id,name:project.name,organizationId:project.organization_id,systemDomain:project.system_domain,client:project.client,currency:project.currency||"SAR",initialStatus:project.initial_status,manualStatus:project.manual_status,archivedAt:project.archived_at},sourceFingerprint=evidence.fingerprint,latest=await latestQuotation(db,project.id),quote=latest?.evidence_fingerprint===sourceFingerprint?latest:null,issueCount=quote?Number((await db.prepare("SELECT COUNT(*) count FROM project_quotation_issues WHERE quotation_revision_id=?").bind(quote.id).first())?.count||0):0,facts={...baseFacts,quotationDrafts:quote?1:0,quotationApproved:quote&&["Approved","Issued"].includes(quote.status)?1:0,quotationIssued:issueCount};return {project:projectView,totals,sourceFingerprint,evidenceManifest:evidence.manifest,currentQuotation:quote,latestQuotation:latest,quotationStale:Boolean(latest&&!quote),workflow:derivePresalesWorkflow({project:projectView,facts})};};
const persistSnapshot=async(db,context,userId)=>{const fingerprint=await digest({version:PRESALES_WORKFLOW_VERSION,project:context.project.id,source:context.sourceFingerprint,stages:context.workflow.stages.map(stage=>[stage.id,stage.status,stage.progress,stage.blockers])}),id=`presalesSnapshot_${fingerprint.slice(0,28)}`;await db.prepare("INSERT OR IGNORE INTO presales_workflow_snapshots (id,project_id,model_version,input_fingerprint,status,progress,current_stage_id,ready_for_quotation,ready_for_issue,stages_json,blockers_json,warnings_json,calculated_by) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(id,context.project.id,PRESALES_WORKFLOW_VERSION,fingerprint,context.workflow.status,context.workflow.progress,context.workflow.currentStageId,context.workflow.readyForQuotation?1:0,context.workflow.readyForIssue?1:0,JSON.stringify(context.workflow.stages),JSON.stringify(context.workflow.blockers),JSON.stringify(context.workflow.warnings),userId).run();return id;};

export async function handlePresalesWorkflowApi(request,env){const url=new URL(request.url),match=url.pathname.match(/^\/api\/projects\/([^/]+)\/presales-workflow(?:\/(refresh|quotation\/draft|quotation\/approve|quotation\/issue))?$/);if(!match)return null;if(!env.DB)return json({error:{code:"PRESALES_STORAGE_UNAVAILABLE",message:"Pre-sales workflow storage is unavailable."}},503);const auth=await authenticateLibraryActor(request,env);if(auth.error)return json({error:auth.error},auth.error.status);const actor=auth.actor,project=await access(env.DB,decodeURIComponent(match[1]),actor.id);if(!project)return json({error:{code:"PROJECT_NOT_FOUND",message:"Project was not found or is not available to this account."}},404);const operation=match[2]||"get",context=await loadPresalesWorkflowContext(env.DB,project);
 if(request.method==="GET"&&operation==="get")return json({...context,role:project.role||"No Project Permission",updatedAt:now()});
 if(request.method!=="POST")return json({error:{code:"METHOD_NOT_ALLOWED",message:"Use the documented workflow operation method."}},405);
 if(operation==="refresh"){const snapshotId=await persistSnapshot(env.DB,context,actor.id);return json({...context,snapshotId,refreshedAt:now()});}
 if(operation==="quotation/draft"){if(!context.workflow.readyForQuotation)return json({error:{code:"QUOTATION_READINESS_BLOCKED",message:"Resolve every technical and commercial blocker before creating a quotation revision.",blockers:context.workflow.blockers}},409);const payload=await bodyOf(request),createdAt=now(),vatBasisPoints=Math.max(0,Math.min(10000,Number(payload.vatBasisPoints??1500))),vatMinor=Math.round(context.totals.subtotalMinor*vatBasisPoints/10000),totalMinor=context.totals.subtotalMinor+vatMinor,termsResult=buildQuotationTerms({payload,project,actorId:actor.id,timestamp:createdAt}),terms=termsResult.terms,quotationFingerprint=await digest({evidenceFingerprint:context.sourceFingerprint,totals:context.totals,vatBasisPoints,terms}),existing=await quotationState(env.DB,project.id,quotationFingerprint);if(existing)return json({quotation:existing,idempotent:true});const snapshotId=await persistSnapshot(env.DB,context,actor.id),revision=Number((await env.DB.prepare("SELECT COALESCE(MAX(revision_number),0)+1 revision FROM project_quotation_revisions WHERE project_id=?").bind(project.id).first())?.revision||1),id=uid("quotationRevision");await env.DB.batch([env.DB.prepare("UPDATE project_quotation_revisions SET superseded_at=? WHERE project_id=? AND superseded_at IS NULL AND status='Draft'").bind(createdAt,project.id),env.DB.prepare("INSERT INTO project_quotation_revisions (id,project_id,revision_number,quotation_fingerprint,workflow_snapshot_id,currency,subtotal_minor,vat_basis_points,vat_minor,total_minor,terms_json,source_summary_json,status,created_by,evidence_fingerprint,evidence_manifest_json,terms_provenance_json) VALUES (?,?,?,?,?,?,?,?,?,?,?,?, 'Draft',?,?,?,?)").bind(id,project.id,revision,quotationFingerprint,snapshotId,context.totals.currency,context.totals.subtotalMinor,vatBasisPoints,vatMinor,totalMinor,JSON.stringify(terms),JSON.stringify({facts:context.workflow.facts,pricingLineCount:context.totals.lineCount,evidenceFingerprint:context.sourceFingerprint}),actor.id,context.sourceFingerprint,JSON.stringify(context.evidenceManifest),JSON.stringify(termsResult.provenance)),env.DB.prepare("INSERT INTO project_quotation_decisions (id,project_id,quotation_revision_id,action,previous_status,next_status,reason,actor_user_id,actor_role,quotation_fingerprint) VALUES (?,?,?,?,NULL,'Draft',?,?,?,?)").bind(uid("quotationDecision"),project.id,id,"Create Draft","Created from the exact governed evidence manifest",actor.id,project.role||"Estimator",quotationFingerprint)]);return json({quotation:{id,revisionNumber:revision,status:"Draft",quotationFingerprint,evidenceFingerprint:context.sourceFingerprint,currency:context.totals.currency,subtotalMinor:context.totals.subtotalMinor,vatMinor,totalMinor,terms,termsProvenance:termsResult.provenance},idempotent:false},201);}
 const commercialRoles=["Commercial Reviewer","Commercial Approver","Project Manager","Management","Administrator"];
 if(!commercialRoles.includes(project.role||""))return json({error:{code:"COMMERCIAL_APPROVAL_PERMISSION_REQUIRED",message:"Commercial approval permission is required."}},403);const payload=await bodyOf(request),reason=String(payload.reason||"").trim();if(reason.length<10)return json({error:{code:"QUOTATION_REASON_REQUIRED",message:"Provide a substantive quotation decision reason."}},422);const quotation=await env.DB.prepare("SELECT * FROM project_quotation_revisions WHERE id=? AND project_id=? AND superseded_at IS NULL").bind(String(payload.quotationRevisionId||""),project.id).first();if(!quotation)return json({error:{code:"QUOTATION_REVISION_NOT_FOUND",message:"Quotation revision not found."}},404);
 if(operation==="quotation/approve"){if(!context.workflow.readyForQuotation||quotation.evidence_fingerprint!==context.sourceFingerprint||quotation.quotation_fingerprint!==String(payload.quotationFingerprint||""))return json({error:{code:"QUOTATION_STALE",message:"The quotation no longer matches current technical or commercial records."}},409);if(["Approved","Issued"].includes(quotation.status))return json({quotation,idempotent:true});if(quotation.status!=="Draft")return json({error:{code:"QUOTATION_STATUS_INVALID",message:"Only a current Draft quotation can be approved."}},409);const approvedAt=now();try{await env.DB.batch([env.DB.prepare("INSERT INTO project_quotation_decisions (id,project_id,quotation_revision_id,action,previous_status,next_status,reason,actor_user_id,actor_role,quotation_fingerprint) VALUES (?,?,?,'Approve','Draft','Approved',?,?,?,?)").bind(uid("quotationDecision"),project.id,quotation.id,reason,actor.id,project.role,quotation.quotation_fingerprint),env.DB.prepare("UPDATE project_quotation_revisions SET status='Approved',approved_at=? WHERE id=? AND status='Draft' AND evidence_fingerprint=? AND quotation_fingerprint=?").bind(approvedAt,quotation.id,context.sourceFingerprint,quotation.quotation_fingerprint)]);}catch{return json({error:{code:"QUOTATION_APPROVAL_STALE",message:"The quotation approval lost a concurrent authority check. Refresh and retry."}},409);}return json({quotationRevisionId:quotation.id,status:"Approved",approvedAt,idempotent:false});}
 if(operation==="quotation/issue"){if(quotation.status==="Issued")return json({quotation,idempotent:true});if(quotation.status!=="Approved")return json({error:{code:"QUOTATION_NOT_APPROVED",message:"Approve the current quotation revision before issue."}},409);const exportJob=await env.DB.prepare("SELECT * FROM excel_export_jobs WHERE id=? AND project_id=?").bind(String(payload.exportJobId||""),project.id).first(),eligibility=exportEligibleForQuotationIssue({quotation,exportJob,currentEvidenceFingerprint:context.sourceFingerprint});if(!eligibility.eligible)return json({error:{code:"GOVERNED_EXPORT_REQUIRED",message:"A current governed export bound to this quotation is required before final issue.",reasons:eligibility.reasons}},409);const issueId=uid("quotationIssue"),issuedAt=now();try{await env.DB.batch([env.DB.prepare("INSERT INTO project_quotation_issues (id,project_id,quotation_revision_id,export_job_id,issue_reference,recipient,transmission_method,issued_by,issued_at,notes) VALUES (?,?,?,?,?,?,?,?,?,?)").bind(issueId,project.id,quotation.id,exportJob.id,String(payload.issueReference||`Q-${project.id}-R${quotation.revision_number}`),String(payload.recipient||project.client||""),String(payload.transmissionMethod||"Controlled Export"),actor.id,issuedAt,reason),env.DB.prepare("UPDATE project_quotation_revisions SET status='Issued',issued_at=? WHERE id=? AND status='Approved' AND evidence_fingerprint=? AND quotation_fingerprint=?").bind(issuedAt,quotation.id,context.sourceFingerprint,quotation.quotation_fingerprint),env.DB.prepare("INSERT INTO project_quotation_decisions (id,project_id,quotation_revision_id,action,previous_status,next_status,reason,actor_user_id,actor_role,quotation_fingerprint) VALUES (?,?,?,'Issue','Approved','Issued',?,?,?,?)").bind(uid("quotationDecision"),project.id,quotation.id,reason,actor.id,project.role,quotation.quotation_fingerprint)]);}catch{return json({error:{code:"QUOTATION_ISSUE_STALE",message:"The quotation issue lost a concurrent authority check. Refresh and retry."}},409);}return json({quotationRevisionId:quotation.id,issueId,status:"Issued",issuedAt,idempotent:false});}
 return json({error:{code:"PRESALES_OPERATION_NOT_FOUND",message:"Pre-sales workflow operation not found."}},404);
}
